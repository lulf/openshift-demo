apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    type: echo
spec:
  containers:
    - name: mypod
      image: lulf/openshift-echo:latest
      ports:
        - name: echo
          containerPort: 2000
